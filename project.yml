name: DrinkTrack
options:
  bundleIdPrefix: com.polidisio
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "15.0"

settings:
  base:
    SWIFT_VERSION: "5.0"
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"

targets:
  MIConsumoBar:
    type: application
    platform: iOS
    sources:
      - path: MIConsumoBar
        excludes:
          - "**/*.md"
    settings:
      base:
        INFOPLIST_FILE: DrinkTrack-Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.polidisio.DrinkTrack
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
    dependencies: []

  DrinkTrackTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: MIConsumoBarTests
    settings:
      base:
        INFOPLIST_FILE: MIConsumoBarTests/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.polidisio.DrinkTrackTests
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/MIConsumoBar.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/MIConsumoBar"
        BUNDLE_LOADER: "$(TEST_HOST)"
    dependencies:
      - target: MIConsumoBar

schemes:
  MIConsumoBar:
    build:
      targets:
        MIConsumoBar: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - DrinkTrackTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
